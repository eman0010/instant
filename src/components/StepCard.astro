---
const { text, class: extraClass } = Astro.props;
---

<div class={`step-card flex flex-col items-center text-center lg:text-left ${extraClass}`}>
  
    <p class="step-text text-sm lg:text-base">{text}</p>
</div>